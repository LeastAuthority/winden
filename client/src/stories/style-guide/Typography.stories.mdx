import { Table, useMantineTheme } from "@mantine/core";
import { Meta, Story } from "@storybook/addon-docs";
import ThemeProvider from "../../app/components/providers/ThemeProvider";
import { WormholeContext } from "../../app/components/providers/WormholeProvider";
import FileLabel from "../../app/components/FileLabel";

<Meta title="Style Guide/Typography" />

export const Overview = () => {
  const items = [
    {
      usage: "Header text",
      size: "2.5rem for large screens, 2rem for medium screens, and 1.35rem for small screens",
      weight: "Bold 700",
      color: "#282f39",
      notes: (
        <ul>
          <li>
            Use <code>classes.headerText</code> to apply styles.
          </li>
          <li>
            Font size depends on the screen width. The breakpoints are based on
            when the longest title of a card will start to word wrap. This lets
            us keep the titles to fit in one line.
          </li>
        </ul>
      ),
    },
    {
      usage: "Left-aligned text [sub header]",
      size: "16px",
      weight: "Light 300",
      color: "#6d6d6d",
      notes: "",
    },
    {
      usage: "Centered text [Instructions/status]",
      size: "16px",
      weight: "Bold 700",
      color: "#6d6d6d",
      notes: "",
    },
    {
      usage: "File card",
      size: "16px",
      weight: "Medium 500",
      color: "#6d6d6d",
      notes: "",
    },
    {
      usage: "Primary button [CTA-like]",
      size: "16px Sentence case",
      weight: "Semi-bold 600",
      color: "#282f39",
      notes: "",
    },
    {
      usage: "Primary button [CTA-like] inactive",
      size: "16px Sentence case",
      weight: "Semi-bold 600",
      color: "#6d6d6d",
      notes: "",
    },
    {
      usage: "Secondary button [medium grey background]",
      size: "16px Sentence case",
      weight: "Semi-bold 600",
      color: "#282f39",
      notes: (
        <>
          Use <code>classes.secondary</code> to apply styles.
        </>
      ),
    },
    {
      usage: "Navigation button",
      size: "16px all caps",
      weight: "Semi-bold 600",
      color: "#282f39",
      notes: "",
    },
    {
      usage: "Form field [code]",
      size: "16px all lowercase [remove https://]",
      weight: "Medium 500",
      color: "#282f39",
      notes: "",
    },
    {
      usage: "Form field when empty [code]",
      size: "16px all lowercase [remove https://]",
      weight: "Medium 500",
      color: "#6d6d6d",
      notes: "",
    },
    {
      usage: "Form label [below field]",
      size: "14px",
      weight: "Light 300 ",
      color: "#6d6d6d",
      notes: "",
    },
    {
      usage: "Footer menu",
      size: "16px on large screens, 14px on small screens",
      weight: "Semi-bold 600",
      color: "#6d6d6d",
      notes: "",
    },
  ];
  const rows = items.map((item) => (
    <tr key={item.usage}>
      <td>{item.usage}</td>
      <td>{item.size}</td>
      <td>{item.weight}</td>
      <td style={{ backgroundColor: item.color, color: "white" }}>
        {item.color}
      </td>
      <td>{item.notes}</td>
    </tr>
  ));
  return (
    <Table>
      <thead>
        <tr>
          <th>Usage</th>
          <th>Size</th>
          <th>Weight</th>
          <th>Color</th>
          <th>Notes</th>
        </tr>
      </thead>
      <tbody>{rows}</tbody>
    </Table>
  );
};

# Typography

## Font

We use [Poppins](https://fonts.google.com/specimen/Poppins) for all fonts.

The base font size is `16px`.

## Overview

<Overview />

## Notes

- We don't follow mantine's color system and instead store a custom color pallete. To use grey text, use `classes.grey` instead of the `color` prop.

```tsx
// right
// - Note: `classes` come from the useCommonStyles hook
<Text className={classes.grey} />

// wrong
<Text color="gray" />
<Text color="dimmed" />
```

- Similarly, use `classes.darkGrey` for darker text, and `classes.dark` for black text.
